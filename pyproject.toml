[project]
name = "agent-family"
version = "0.1.0"
description = "Learning agentic AI with Langchain and LangGraph"
requires-python = ">=3.10"

dependencies = [
    "langchain>=1.1",
    "langgraph>=1.0",
    "langchain-openai>=1.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
]